import { Button } from "@/components/ui/button";
import { CalendarDays, CircleCheck, CircleX, ExternalLink, LockKeyhole, RefreshCcw, ShieldCheck } from "lucide-react";

type CalendarIntegrationWorkspaceProps = { snapshot: any };

export function CalendarIntegrationWorkspace({ snapshot }: CalendarIntegrationWorkspaceProps) {
  const connections = snapshot.integrationConnections ?? [];
  const google = connections.find((connection: any) => connection.provider === "google_calendar");
  const activeEvents = (snapshot.externalEvents ?? []).filter((event: any) => event.status === "active");
  const status = google?.status ?? "not_connected";
  const connected = status === "connected";
  return <section className="connections-workspace" aria-labelledby="connections-heading"><header><div><h2 id="connections-heading">Calendar connections</h2><p>External calendars can contribute read-only busy time to realistic planning. They never create, move, complete, or delete events from this planner.</p></div><CalendarDays size={29} aria-hidden="true" /></header><section className="connection-status"><div className={connected ? "connection-status-icon is-connected" : "connection-status-icon"}>{connected ? <CircleCheck size={24} /> : <CircleX size={24} />}</div><div><h3>{connected ? "Google Calendar connected" : "Google Calendar not connected"}</h3><p>{connected ? `${activeEvents.length} active external busy event${activeEvents.length === 1 ? "" : "s"} are available for workload and proposal calculations.` : "No external calendar data is being read. Your task calendar remains fully local to this workspace."}</p></div><span className={connected ? "connection-pill is-connected" : "connection-pill"}>{connected ? "Read-only busy overlay" : "Setup required"}</span></section><div className="connection-policy"><section><ShieldCheck size={20} /><div><h3>Read-only by design</h3><p>Once authorized, the connection imports busy/free context only. Proposed task reservations still require explicit approval inside Personal Calendar.</p></div></section><section><LockKeyhole size={20} /><div><h3>OAuth stays external</h3><p>Connection setup will require a Google OAuth client, approved redirect URI, consent screen, and your explicit account authorization. No credential is requested or stored in this branch yet.</p></div></section></div><section className="connection-readiness"><div><h3>What will be needed to connect</h3><p>These are prerequisites for the final credential-dependent setup phase. This status page is intentionally non-connecting until they are provided.</p></div><ol><li><span>1</span><div><strong>Google Cloud project</strong><p>Create a project and configure a consent screen for the account that will authorize calendar access.</p></div></li><li><span>2</span><div><strong>OAuth client and redirect URI</strong><p>Provide the client ID, client secret, and the production callback URL for this application.</p></div></li><li><span>3</span><div><strong>Explicit read-only consent</strong><p>Authorize calendar read access. This planner will only read busy periods for availability calculations.</p></div></li></ol></section><section className="connection-recovery"><div><RefreshCcw size={19} /><div><h3>Recovery when a connection expires</h3><p>Keep planning locally. The workspace will show unavailable external context, retain existing task reservations, and ask you to re-authorize—never silently write to the external calendar.</p></div></div><Button type="button" variant="ghost" disabled title="OAuth credentials and explicit consent are required before a connection can be started."><ExternalLink size={15} /> Connection setup opens in the final phase</Button></section></section>;
}
