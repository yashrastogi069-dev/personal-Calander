import { Button } from "@/components/ui/button";
import { CalendarDays, CircleCheck, CircleX, ExternalLink, LockKeyhole, RefreshCcw, ShieldCheck } from "lucide-react";

type CalendarIntegrationWorkspaceProps = { snapshot: any };

export function CalendarIntegrationWorkspace({ snapshot }: CalendarIntegrationWorkspaceProps) {
  const connections = snapshot.integrationConnections ?? [];
  const google = connections.find((connection: any) => connection.provider === "google_calendar");
  const activeEvents = (snapshot.externalEvents ?? []).filter((event: any) => event.status === "active");
  const oauthConnected = google?.status === "connected";
  const icsOverlay = snapshot.icsOverlay ?? { status: "unconfigured", label: "Secure ICS overlay not configured", message: "No external calendar data is being read." };
  const icsReady = icsOverlay.status === "ready";
  return <section className="connections-workspace" aria-labelledby="connections-heading"><header><div><h2 id="connections-heading">Calendar connections</h2><p>External calendars can contribute read-only busy time to realistic planning. They never create, move, complete, or delete events from this planner.</p></div><CalendarDays size={29} aria-hidden="true" /></header><section className="connection-status"><div className={icsReady || oauthConnected ? "connection-status-icon is-connected" : "connection-status-icon"}>{icsReady || oauthConnected ? <CircleCheck size={24} /> : <CircleX size={24} />}</div><div><h3>{icsOverlay.label}</h3><p>{activeEvents.length ? `${activeEvents.length} active external busy event${activeEvents.length === 1 ? "" : "s"} are available for workload and reservation conflict checks.` : icsOverlay.message}</p></div><span className={icsReady || oauthConnected ? "connection-pill is-connected" : "connection-pill"}>{activeEvents.length ? "Read-only busy overlay" : icsReady ? "Refresh not enabled" : "Setup required"}</span></section><div className="connection-policy"><section><ShieldCheck size={20} /><div><h3>Read-only by design</h3><p>ICS events are busy context only. Task reservation, completion, and removal remain local planner actions; this application never writes back to the source calendar.</p></div></section><section><LockKeyhole size={20} /><div><h3>Secret stays server-side</h3><p>A Google private iCal address belongs only in secure server configuration. It is never shown here, sent to the browser, stored in planner data, or included in calendar event payloads.</p></div></section></div><section className="connection-readiness"><div><h3>Two deliberate connection paths</h3><p>The ICS overlay does not require OAuth. OAuth remains a separate future option; neither path can be started from this branch without the final credential and consent stage.</p></div><ol><li><span>1</span><div><strong>Secure Google ICS overlay</strong><p>{icsReady ? "The approved server-only source shape is configured. A later explicit refresh implementation will validate destination, response size, redirects, and parsed events before any busy context appears." : "Add a Google Calendar secret iCal URL through a server-only environment value only when you choose to activate it. Until then, no source is read."}</p></div></li><li><span>2</span><div><strong>Optional Google OAuth</strong><p>{oauthConnected ? "An existing read-only Google connection is available." : "OAuth is optional and remains unavailable until its separate client, redirect URI, and explicit account consent are supplied."}</p></div></li><li><span>3</span><div><strong>Visible recovery</strong><p>Invalid configuration or a future refresh error leaves task reservations untouched and shows that external busy context is unavailable rather than using stale or hidden data.</p></div></li></ol></section><section className="connection-recovery"><div><RefreshCcw size={19} /><div><h3>Configuration-gated and non-connecting</h3><p>No feed URL is collected through the browser and no network request runs from this page. The displayed status contains no secret address or source token.</p></div></div><Button type="button" variant="ghost" disabled title="An approved secure environment value and an explicit final activation step are required before refresh can be implemented."><ExternalLink size={15} /> ICS refresh is not enabled</Button></section></section>;
}
