import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { trpc } from "@/lib/trpc";
import type { WorkspaceScope } from "@/lib/workspace";
import { zonedDateTimeToUtc } from "@shared/planningAvailability";
import { parseNaturalLanguageTask, type NaturalLanguageTaskDraft } from "@shared/naturalLanguageTask";
import { CalendarClock, CircleAlert, PenLine, Sparkles } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

type NaturalLanguageCaptureWorkspaceProps = { scope: WorkspaceScope; today: string; snapshot: any };

function addMinutes(localDate: string, time: string, minutes: number, timezone: string) {
  const [hours, minutesValue] = time.split(":").map(Number);
  const startMinutes = hours * 60 + minutesValue;
  return { plannedStartAt: zonedDateTimeToUtc(localDate, startMinutes, timezone), plannedEndAt: zonedDateTimeToUtc(localDate, startMinutes + minutes, timezone) };
}

export function NaturalLanguageCaptureWorkspace({ scope, today, snapshot }: NaturalLanguageCaptureWorkspaceProps) {
  const utils = trpc.useUtils();
  const [thought, setThought] = useState("");
  const [draft, setDraft] = useState<NaturalLanguageTaskDraft | null>(null);
  const [templateName, setTemplateName] = useState("");
  const [error, setError] = useState<string | null>(null);
  const create = trpc.planner.task.create.useMutation({ onSuccess: () => { setThought(""); setDraft(null); setError(null); utils.planner.workspace.snapshot.invalidate(); utils.planner.dashboard.invalidate(); toast.success("Task created from your reviewed capture."); }, onError: mutationError => setError(mutationError.message || "The reviewed task could not be created.") });
  const saveTemplate = trpc.planner.planningTemplate.create.useMutation({ onSuccess: () => { setTemplateName(""); setError(null); utils.planner.workspace.snapshot.invalidate(); toast.success("Task starting point saved. It has not created work."); }, onError: mutationError => setError(mutationError.message || "The task starting point could not be saved.") });
  const archiveTemplate = trpc.planner.planningTemplate.archive.useMutation({ onSuccess: () => { setError(null); utils.planner.workspace.snapshot.invalidate(); toast.success("Task starting point archived."); }, onError: mutationError => setError(mutationError.message || "The task starting point could not be archived.") });
  const parse = () => { if (!thought.trim()) { setError("Write a task thought first; parsing does not create work on its own."); return; } setError(null); setDraft(parseNaturalLanguageTask(thought, today)); };
  const createTask = () => {
    if (!draft) return;
    if (!draft.title.trim()) { setError("Give the reviewed task a clear title before creating it."); return; }
    const canReserve = draft.scheduledLocalDate && draft.reserveTime && draft.estimateMinutes;
    const reservation: { plannedStartAt?: Date; plannedEndAt?: Date } = canReserve ? addMinutes(draft.scheduledLocalDate!, draft.reserveTime!, draft.estimateMinutes!, scope.timezone) : {};
    setError(null);
    create.mutate({ ...scope, title: draft.title.trim(), priority: draft.priority, horizon: "weekly", dueLocalDate: draft.dueLocalDate, scheduledLocalDate: draft.scheduledLocalDate, estimateMinutes: draft.estimateMinutes, plannedStartAt: reservation.plannedStartAt ?? null, plannedEndAt: reservation.plannedEndAt ?? null, recurrenceRule: draft.recurrenceRule, recurrenceAnchor: draft.recurrenceRule ? "scheduled" : null });
  };
  const taskTemplates = (snapshot.planningTemplates ?? []).filter((template: any) => template.kind === "task" && !template.archivedAt).slice(0, 12);
  const useTemplate = (template: any) => { const payload = template.payload ?? {}; setDraft({ ...parseNaturalLanguageTask("", today), title: typeof payload.title === "string" ? payload.title : "", priority: ["none", "low", "medium", "high", "critical"].includes(payload.priority) ? payload.priority : "medium", dueLocalDate: typeof payload.dueLocalDate === "string" ? payload.dueLocalDate : null, scheduledLocalDate: typeof payload.scheduledLocalDate === "string" ? payload.scheduledLocalDate : null, reserveTime: typeof payload.reserveTime === "string" ? payload.reserveTime : null, estimateMinutes: typeof payload.estimateMinutes === "number" ? payload.estimateMinutes : null, recurrenceRule: payload.recurrenceRule && typeof payload.recurrenceRule === "object" ? payload.recurrenceRule : null, notes: ["Starting point loaded. Review and edit it before creating a task."] }); };
  return <section className="capture-workspace" aria-labelledby="capture-workspace-heading"><header><div><h2 id="capture-workspace-heading">Capture the thought. Review the plan.</h2><p>Type naturally. The parser reads only clear phrases, exposes every result, and never creates or reserves time until you choose Create task.</p></div><Sparkles size={28} aria-hidden="true" /></header>{error ? <div className="capture-error" role="alert"><CircleAlert size={16} /><span>{error}</span><button type="button" onClick={() => setError(null)} aria-label="Dismiss capture message">×</button></div> : null}<section className="capture-input-panel"><Label htmlFor="natural-task">What needs your attention?</Label><textarea id="natural-task" value={thought} onChange={event => setThought(event.target.value)} maxLength={1000} placeholder="Finish report tomorrow at 2pm for 45 minutes high priority" /><div><p>Examples: “Pay invoice by Friday”, “Review metrics every Monday and Wednesday”, or “Send proposal tomorrow for 30 min”.</p><Button type="button" onClick={parse}><PenLine size={15} /> Parse for review</Button></div></section>{taskTemplates.length ? <section className="capture-template-shelf" aria-labelledby="task-starting-points-heading"><div><h3 id="task-starting-points-heading">Task starting points</h3><p>Load defaults into the review form. A starting point cannot create a task by itself.</p></div><div>{taskTemplates.map((template: any) => <article key={template.id}><span><strong>{template.name}</strong><small>{template.description || "Review before use"}</small></span><button type="button" onClick={() => useTemplate(template)}>Use</button><button type="button" onClick={() => archiveTemplate.mutate({ ...scope, id: template.id, expectedVersion: template.version })} disabled={archiveTemplate.isPending} aria-label={`Archive ${template.name}`}>Archive</button></article>)}</div></section> : null}{draft ? <section className="capture-draft-panel" aria-labelledby="capture-draft-heading"><div className="capture-draft-heading"><div><h3 id="capture-draft-heading">Review parsed details</h3><p>Edit anything before it becomes a task. A blank field stays blank; nothing is guessed.</p></div><CalendarClock size={21} aria-hidden="true" /></div><div className="capture-draft-fields"><label>Task title<Input value={draft.title} onChange={event => setDraft(current => current ? { ...current, title: event.target.value } : current)} /></label><label>Priority<select value={draft.priority} onChange={event => setDraft(current => current ? { ...current, priority: event.target.value as NaturalLanguageTaskDraft["priority"] } : current)}><option value="none">No priority</option><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select></label><label>Deadline<Input value={draft.dueLocalDate ?? ""} type="date" onChange={event => setDraft(current => current ? { ...current, dueLocalDate: event.target.value || null } : current)} /></label><label>Plan for<Input value={draft.scheduledLocalDate ?? ""} type="date" onChange={event => setDraft(current => current ? { ...current, scheduledLocalDate: event.target.value || null } : current)} /></label><label>Reserve time<Input value={draft.reserveTime ?? ""} type="time" onChange={event => setDraft(current => current ? { ...current, reserveTime: event.target.value || null } : current)} /></label><label>Focus time needed<Input value={draft.estimateMinutes ? String(draft.estimateMinutes) : ""} type="number" min="5" max="1440" onChange={event => { const value = Number(event.target.value); setDraft(current => current ? { ...current, estimateMinutes: event.target.value && Number.isInteger(value) && value >= 5 && value <= 1440 ? value : null } : current); }} placeholder="Minutes" /></label><label>Repeats<select value={draft.recurrenceRule?.frequency ?? "none"} onChange={event => setDraft(current => current ? { ...current, recurrenceRule: event.target.value === "none" ? null : { frequency: event.target.value as "daily" | "weekly" | "monthly", interval: 1, ...(event.target.value === "weekly" ? { weekdays: [1] } : {}) } } : current)}><option value="none">Does not repeat</option><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select></label></div>{draft.notes.length ? <ul className="capture-notes">{draft.notes.map(note => <li key={note}>{note}</li>)}</ul> : null}<div className="capture-reservation-note">{draft.scheduledLocalDate && draft.reserveTime && draft.estimateMinutes ? `Creating this task will reserve ${draft.estimateMinutes} minutes on ${draft.scheduledLocalDate} at ${draft.reserveTime} (${scope.timezone}).` : "A calendar reservation is created only when Plan for, Reserve time, and Focus time needed are all supplied."}</div><div className="capture-template-save"><Label htmlFor="capture-template-name">Save these defaults as a starting point</Label><div><Input id="capture-template-name" value={templateName} onChange={event => setTemplateName(event.target.value)} maxLength={120} placeholder="Example: Admin follow-up" /><Button type="button" variant="ghost" onClick={() => { if (!templateName.trim()) { setError("Name the starting point before saving it."); return; } saveTemplate.mutate({ ...scope, kind: "task", name: templateName.trim(), description: "Reviewed task defaults; does not create work.", payload: { title: draft.title, priority: draft.priority, estimateMinutes: draft.estimateMinutes, recurrenceRule: draft.recurrenceRule } }); }} disabled={saveTemplate.isPending}>{saveTemplate.isPending ? "Saving…" : "Save starting point"}</Button></div></div><div className="capture-draft-actions"><Button type="button" variant="ghost" onClick={() => setDraft(null)}>Discard draft</Button><Button type="button" className="primary-action" onClick={createTask} disabled={create.isPending}>{create.isPending ? "Creating…" : "Create task"}</Button></div></section> : null}</section>;
}
